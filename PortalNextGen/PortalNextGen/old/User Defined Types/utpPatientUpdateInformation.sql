CREATE TYPE [old].[utpPatientUpdateInformation] AS TABLE (
    [DeviceID]         INT           NOT NULL,
    [PatientSessionID] INT           NOT NULL,
    [ID1]              NVARCHAR (30) NOT NULL);

